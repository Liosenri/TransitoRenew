import React from 'react';
import {View, Text} from 'react-native';

interface Props {}

const index = ({}: Props) => {
  return (
    <View>
      <Text>Login</Text>
    </View>
  );
};

export default index;
